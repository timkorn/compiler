const g = new Dracula.Graph
for (var j = 0; j < i; i++) {
    n += i
}
g.addEdge('Tim', 'Timooon')
const layouter = new Dracula.Layout.Spring(g)
const renderer = new Dracula.Renderer.Raphael('#canvas', g, 1500, 600)
renderer.draw()


